# Sample Braingent Memory

This directory contains synthetic task data for the public dashboard example.

Set `BRAINGENT_MEMORY_ROOT=/path/to/your/braingent` to point the dashboard at a
real memory repo. Keep private task data out of public examples.
