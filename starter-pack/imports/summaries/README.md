# Import Summaries

Curated summaries created from raw sources.

Use `templates/import-summary-record.md`.

The summary should become the durable source of truth. Raw files should be retained only when needed.

