# Archived Live Tasks

Move closed live tasks here by month, for example:

```text
archive/
`-- 2026-01/
    `-- BGT-0001--example-review-task.md
```

Archive files are still Markdown, but completed work that matters should also have durable records outside `tasks/`.
