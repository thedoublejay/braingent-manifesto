# Claude Instructions

This repository is durable engineering memory. Follow `AGENTS.md`.

Search with `braingent find` before planning. Read `CURRENT_STATE.md` for the generated now-page. Hydrate records at `depth="summary"` unless evidence requires `full`. Do not load `indexes/records.md` or `INDEX.md` as a scan surface.

If facts change, write a new record with repo-relative `supersedes` / `superseded_by` links.
