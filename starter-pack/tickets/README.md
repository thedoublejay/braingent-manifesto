# Tickets

Use this directory for cross-cutting ticket stubs.

Create a ticket stub only when one ticket spans multiple projects or repositories.

Recommended structure:

```text
tickets/ticket--<system>--<id>/README.md
```

For single-repository work, the `ticket:` frontmatter field in the task record is usually enough.

